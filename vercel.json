{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.18.3 --activate && pnpm -w install --frozen-lockfile && pnpm --filter @archei/web build",
  "installCommand": "corepack enable && corepack prepare pnpm@10.18.3 --activate && pnpm -w install --frozen-lockfile",
  "outputDirectory": "apps/web/.next"
}
